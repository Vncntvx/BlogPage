---
import { DEFAULT_LOCALE } from "@/i18n/config";
import { getLocalePath } from "@/i18n/utils";

export const prerender = true;

return Astro.redirect(`${getLocalePath(DEFAULT_LOCALE.code)}/archives`.replace(/\/+/, "/"), 308);
---
